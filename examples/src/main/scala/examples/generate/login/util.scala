//comment package statement to generate
package examples.generate.login

object util {
  def validateAuth(uname: String, pwd: String): Boolean = {
    true
  }
  def validateId(id: String, uname: String): Boolean = {
    true
  }
}
